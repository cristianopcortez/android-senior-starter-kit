package br.com.ccortez.seniorstarterkitapplication.data

/**
 * Generic, non-localized error copy for the data layer when mapping to [Resource.Error].
 * User-facing strings belong in `presentation` / `strings.xml`.
 */
internal object ErrorMessages {
    const val UNKNOWN_ERROR = "Unknown error"
}
